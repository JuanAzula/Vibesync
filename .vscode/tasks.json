{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start-dev-server",
            "type": "shell",
            "command": "pnpm run dev & pnpm data",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}